// since state isn't active initially, we pass in a default init state which is empty
const initState = {}

// create a function to take in the state and action parameters: the state takes in initState
const authReducer = (state = initState, action) => {
    return state
}

export default authReducer 

